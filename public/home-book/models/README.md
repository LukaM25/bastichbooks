`book-shell.glb` is intentionally not sourced yet.

Reason:
- a custom model or procedural mesh will give tighter control over proportions, hinge placement,
  inner page surfaces, and interactions than a generic downloaded book model.

Target file when ready:
- `public/home-book/models/book-shell.glb`
